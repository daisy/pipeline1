package org.daisy.util.fileset.interfaces;

/**
 * Empty interface that all <code>FilesetFile</code>s that represent
 * schemas (DTDs, RelaxNGs, etc) must implement. 
 * @author Markus Gylling
 */
public interface SchemaFile {

}
